version: '3.8'
services:
  proposal-generator:
    build: .
    container_name: proposal-generator
    ports:
      - "8000:8000"
    restart: always
    volumes:
      - ./backend/data:/app/backend/data
